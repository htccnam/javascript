# lý thuyết
Map là một cấu trúc dữ liệu cho phép lưu trữ các cặp Key - Value (Khóa - Giá trị), trong đó Key có thể là bất kỳ kiểu dữ liệu nào (nguyên thể, object, hàm, v.v.).
# hãy khai báo 1 map rỗng bằng map và in ra để xem kết quả
